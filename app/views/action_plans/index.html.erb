<p id="notice"><%= notice %></p>

<h1>Planos de ação</h1>

<table>
  <thead>
    <tr>
      <th>Data</th>
      <th>Evolucao</th>
      <th>Aluno</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @action_plans.each do |action_plan| %>
      <% estudante = action_plan.student.nome %>
      <tr>
        <td><%= l action_plan.data %></td>
        <td><%= action_plan.evolucao %></td>
        <td><%= estudante %></td>
        <td><%= link_to t('Show'), action_plan %></td>
        <td><%= link_to t('Edit'), edit_action_plan_path(action_plan) %></td>
        <td><%= link_to t('Destroy'), action_plan, method: :delete, data: { confirm: 'Deseja relamente excluir?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to t('New Action Plan'), new_action_plan_path %>
