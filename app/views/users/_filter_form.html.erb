<div class="card">
  <div class="card-body">
    <%= form_for_filterrific filterrific, html: { id: 'filterrific-no-ajax-auto-submit' } do |f| %>
      <div class="form-group">
        <%= f.label :search_query, 'Pesquisar', class: 'form-control-label' %>
        <%= f.text_field :search_query, class: 'form-control mb-4 mr-sm-4 mb-sm-0' %>
      </div>

      <div class="form-group">
        <%= f.label :search_query, 'Função', class: 'form-control-label' %>
        <%= f.select :sorted_by_role, filterrific.select_options[:sorted_by_role], { prompt: true }, { class: 'custom-select form-control' } %>
      </div>

      <div class="form-group">
        <%= f.label :search_query, 'Ordernar', class: 'form-control-label' %>
        <%= f.select :sorted_by, filterrific.select_options[:sorted_by], {}, { class: 'custom-select form-control' } %>
      </div>

      <%= f.submit 'Filtrar', class: 'btn btn-primary' %>
      <%= link_to 'Limpar', reset_filterrific_url, class: 'btn btn-secondary' %>
    <% end %>
  </div>
</div>
